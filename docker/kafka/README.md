# Kafka

Stack Kafka gồm `zookeeper`, `kafka`, `kafka-ui`.

## Start

```bash
bash docker/kafka/start.sh
```

Hoặc từ thư mục gốc:

```bash
docker compose up -d zookeeper kafka kafka-ui
```

## Web UI

http://localhost:8080

Xem thêm: [../README.md](../README.md)
